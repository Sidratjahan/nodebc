// Create a new index27.js file

// Define a firstNumber and secondNumber variables
let firstNumber, secondNumber;

// Assign each variable a number type value
firstNumber = 18;
secondNumber = 9;


// Code the following operations:

// Adding both variables
let adding = firstNumber+ secondNumber;
// Subtracting both variables
let subtracting = firstNumber-secondNumber;

// Multiplying both variables
let multiplying = firstNumber*secondNumber;

// Dividing both variables
let dividing = firstNumber/secondNumber;
// Remainder from both variables
let remainder = firstNumber % secondNumber;
// For each operation show the following output:
let template1 = `I add ${firstNumber}+${secondNumber} and the result is ${adding}`;
let template2 = `I subtract ${firstNumber}-${secondNumber} and the result is ${subtracting}`;
let template3 = `I multiply ${firstNumber}*${secondNumber} and the result is ${multiplying}`;
let template4 = `I divide ${firstNumber}/${secondNumber} and the result is ${dividing}`;
let template5 = `I found the remainder ${remainder} from ${firstNumber}%${secondNumber}`;

// Example: I add 2 + 3 and the result is 5 (where 2 and 3 are firstNumber and secondNumber and 5 it's the operation result)
// Use a template variable for each output and console.log()
console.log(template1);
console.log(template2);
console.log(template3);
console.log(template4);
console.log(template5);