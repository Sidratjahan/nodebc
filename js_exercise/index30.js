// Create a new index30.js file
// Calculate and show the area of a square
// Side length is 5
// Show a descriptive message to show the square area and side length

let sideLength = 5;
let message = `To calculate area of a square, for side length ${sideLength}, area will be ${sideLength*sideLength}`;
console.log(message);
